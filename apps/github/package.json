{
  "name": "initiative-github",
  "version": "1.0.0",
  "private": true,
  "description": "A GitHub organization's issues, reviews and dependency alerts in an Initiative community: endpoints an automation can call, widgets a dashboard can draw.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/src/main.js",
    "typecheck": "tsc --noEmit",
    "test": "node scripts/run-tests.mjs",
    "test:watch": "vitest",
    "manifest": "npm run build && node dist/scripts/build-manifest.js && initiative-app validate manifest.json",
    "manifest:check": "npm run build && node dist/scripts/build-manifest.js --check && initiative-app validate manifest.json"
  },
  "dependencies": {
    "initiative-app-kit": "github:Morelitea/initiative-app-kit#v0.23.1"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  }
}
